<h1> <%= @brand %> </h1>
<p><%= @result %></p>

<ul>
    <% @names.each do |name| %>
        <li><%= name %></li>
        <% end %>
        
</ul>

<h2>
    <%= @person[:first_name] + ' ' + @person[:last_name] %>
</h2>
